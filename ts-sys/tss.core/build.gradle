group 'org.aming'
version '1.0.0'

apply plugin: 'java'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    compile(project(':tss.base'))
    testCompile group: 'junit', name: 'junit', version: '4.12'
}
